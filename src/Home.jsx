import React from 'react';
import ReactDOM from 'react-dom';

var Home = (props) => (
  <div className="Home">
    This is the main page, found in the Home component.
  </div>
);

export default Home;
