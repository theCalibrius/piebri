import React from 'react';
import ReactDOM from 'react-dom';

var Contact = (props) => (
  <div className="Contact">
    This is the contact page, found in the Contact component.
  </div>
);

export default Contact;
